class Solution:
    def search(self, arr, x):
        # code here
        
        for index in range(len(arr)):
            
            if arr[index]==x :
                
                return index
                
        return -1
