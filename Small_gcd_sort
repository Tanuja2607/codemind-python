import math
T = int(input())
for _ in range(T):
    N = int(input())
    players = []
    for i in range(1, N + 1):
        players.append((math.gcd(i, N), i))
    players.sort(key=lambda x: (-x[0], x[1]))
    print(*[i for _, i in players])

