# Coloured Balloons - CodeChef Solution (Python 3)

t = int(input())

for _ in range(t):
    n = int(input())
    A = list(map(int, input().split()))
    
    total_cost = 0
    for i in range(n):
        total_cost += (i + 1) * A[i]
    
    print(total_cost)
