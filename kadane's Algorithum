class Solution:
    def maxSubarraySum(self, arr):
        # Code here
        max_sum = float('-inf')
        s = 0
        for num in arr:
            if s < 0 and s < num:
                s = 0
            s += num
            max_sum = max(max_sum, s)
            
        return max_sum
